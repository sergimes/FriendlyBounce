#ifndef OBJECT_H
#define OBJECT_H

#include "object.hh"

#endif

class Ball : public Object {
    private:

    public:
        //Constructors
        Ball();

        //Destructors
        ~Ball();
};
